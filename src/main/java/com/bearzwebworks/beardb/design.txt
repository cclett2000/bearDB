Pull all data using IDs
    - index in data list is the company's ID
        > when adding contacts, projects add company index to data structure, this will allow query based on table/ID

Date: use JavaFX date picker and see how to collect the date and store it